# Direct dependencies for Binance Trading Bot
requests==2.31.0
typer==0.12.3
click==8.1.7     # Pinned specifically to prevent Typer compatibility issues
rich==13.7.0
python-dotenv==1.0.1

# Development / Testing
pytest==7.4.0